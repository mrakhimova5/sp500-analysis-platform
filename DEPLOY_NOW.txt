🚀 RENDER DEPLOYMENT INSTRUCTIONS
=================================

Your repository is ready! Now deploy to Render:

📋 STEP-BY-STEP:

1. Open Render Dashboard (opening in browser now...)
   URL: https://dashboard.render.com/

2. Click "New +" → "Web Service"

3. Connect GitHub:
   - Click "Connect GitHub" (or use existing connection)
   - Select repository: mrakhimova5/sp500-analysis-platform
   - Click "Connect"

4. Render Auto-Detects Everything! ✅
   (Because render.yaml exists, it auto-fills all settings)
   
   You should see:
   ✓ Name: sp500-analysis
   ✓ Region: Oregon (Free)
   ✓ Branch: main
   ✓ Runtime: Python 3
   ✓ Build: pip install -r requirements.txt
   ✓ Start: gunicorn backend_app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300
   ✓ Plan: FREE

5. Environment Variables (should auto-populate from render.yaml):
   ✓ FLASK_ENV=production
   ✓ DEBUG=False
   ✓ MAX_CONTENT_LENGTH=52428800
   ✓ CORS_ORIGINS=*
   ✓ SECRET_KEY=<auto-generated>

6. Click "Create Web Service"

7. Wait 3-5 minutes for deployment...
   - Watch the build logs
   - You'll see: "Installing dependencies...", "Starting gunicorn..."
   - When done: "Your service is live!"

8. Get Your URL:
   Format: https://sp500-analysis.onrender.com
   (or similar, Render assigns the URL)

9. Copy the URL and test it!

📝 AFTER DEPLOYMENT:

Once you have your Render URL, update frontend.html:

1. Edit frontend.html line ~20:
   const API_URL = 'https://YOUR-RENDER-URL.onrender.com';

2. Commit and push:
   git add frontend.html
   git commit -m "Update API URL for Render"
   git push origin main

3. Render auto-deploys the update!

✅ DONE! Your app is live and FREE!

💰 Cost: $0/month
🌐 Auto-deploy: On every git push
⚡ Performance: Good for demo/portfolio
⏱️ Limitation: 15min idle → sleeps (30s cold start)

🎉 Repository: https://github.com/mrakhimova5/sp500-analysis-platform
🚀 Render: https://dashboard.render.com/
